<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fq.fuqueue">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActiveShoppingList" />
        <activity android:name=".PaypalPayment" />
        <activity android:name=".PaymentPaypalDetails" />
        <activity android:name=".Chosen" />
        <activity android:name=".AllShoppingList" />
        <activity android:name=".BarcodeScanner" />
        <activity android:name=".StoreMap" />
        <activity android:name=".LoginUser"></activity>
    </application>

</manifest>